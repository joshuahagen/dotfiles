#!/bin/sh

is_muted="$(amixer | grep 'Front Left: Playback' | awk '{print $6}')"
if [ "$is_muted" = "[off]" ]; then
	echo "^c#FE7E6D^ Muted" && exit 0
fi

echo "^c#d7bfe8^ ^c#E7D7AD^$(awk -F"[][]" '/Left:/ { print $2 }' <(amixer sget Master))"
